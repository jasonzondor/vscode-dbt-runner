{
  "name": "dbt-runner",
  "displayName": "DBT Runner",
  "description": "VS Code extension for running dbt projects with Snowflake key pair authentication",
  "version": "0.5.2",
  "publisher": "jason-zondor",
  "repository": {
    "type": "git",
    "url": "https://github.com/jasonzondor/code_dbt_runner.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "workspaceContains:**/pyproject.toml"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "dbt-runner.runDbt",
        "title": "DBT Runner: Run DBT Command"
      },
      {
        "command": "dbt-runner.setupProject",
        "title": "DBT Runner: Setup Project (Poetry Install & DBT Deps)"
      },
      {
        "command": "dbt-runner.addSnowflakeAccount",
        "title": "DBT Runner: Add Snowflake Account"
      },
      {
        "command": "dbt-runner.removeSnowflakeAccount",
        "title": "DBT Runner: Remove Snowflake Account"
      },
      {
        "command": "dbt-runner.listSnowflakeAccounts",
        "title": "DBT Runner: List Snowflake Accounts"
      },
      {
        "command": "dbt-runner.runPreCommit",
        "title": "DBT Runner: Run Pre-Commit Checks"
      },
      {
        "command": "dbt-runner.runProjectEvaluator",
        "title": "DBT Runner: Run DBT Project Evaluator"
      }
    ],
    "keybindings": [
      {
        "command": "dbt-runner.runDbt",
        "key": "ctrl+shift+d",
        "mac": "cmd+shift+d"
      }
    ],
    "configuration": {
      "title": "DBT Runner",
      "properties": {
        "dbtRunner.snowflakeAccounts": {
          "type": "array",
          "default": [],
          "markdownDescription": "Configure Snowflake accounts for key pair authentication. Click 'Edit in settings.json' to add accounts.",
          "items": {
            "type": "object",
            "required": ["name", "account", "user", "privateKeyPath"],
            "properties": {
              "name": {
                "type": "string",
                "description": "Display name for this configuration",
                "minLength": 1
              },
              "account": {
                "type": "string",
                "description": "Snowflake account identifier (e.g., xy12345.us-east-1)",
                "minLength": 1
              },
              "user": {
                "type": "string",
                "description": "Snowflake username",
                "minLength": 1
              },
              "privateKeyPath": {
                "type": "string",
                "description": "Absolute path to private key file (.p8)",
                "minLength": 1
              },
              "privateKeyPassphrase": {
                "type": "string",
                "default": "",
                "description": "Private key passphrase (leave empty to be prompted at runtime)"
              }
            }
          },
          "editPresentation": "multilineText"
        },
        "dbtRunner.environments": {
          "type": "array",
          "default": [
            "dev",
            "prod"
          ],
          "markdownDescription": "List of dbt target environments. These should match targets in your `profiles.yml`.",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "minItems": 1,
          "uniqueItems": true
        },
        "dbtRunner.autoSetupOnOpen": {
          "type": "boolean",
          "default": false,
          "markdownDescription": "Automatically run `poetry install` and `dbt deps` when opening a dbt project",
          "order": 3
        },
        "dbtRunner.dbtProjectPath": {
          "type": "string",
          "default": "dbt",
          "markdownDescription": "Relative path to dbt project directory from workspace root",
          "order": 2
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "semantic-release": "semantic-release"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@types/node": "^20.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "@vscode/vsce": "^3.7.1",
    "conventional-changelog-conventionalcommits": "^8.0.0",
    "eslint": "^8.54.0",
    "semantic-release": "^24.0.0",
    "typescript": "^5.3.0"
  }
}
